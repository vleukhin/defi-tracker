/**
 * Дефолты настроек — универсальный модуль: их знает и сервер (когда строки
 * user_settings ещё нет), и клиент (пока /api/settings не ответил).
 *
 * Лежат отдельно от `lib/api/settings`, потому что тот помечен
 * "server-only" и в бандл страницы не идёт, а числа нужны обеим сторонам.
 * Дублировать их в компоненте нельзя: разъехавшись, они дали бы разный
 * расчёт до и после загрузки настроек.
 */

/** Порог предупреждения по health factor (S4.1/S4.3). */
export const DEFAULT_HF_WARNING_THRESHOLD = 1.5;

/** Рабочий уровень плеча по стратегии Capital Growth, % (docs/07 §10.3). */
export const DEFAULT_TARGET_LTV_PCT = 50;
