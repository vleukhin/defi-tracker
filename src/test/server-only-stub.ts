/**
 * Пустой стаб пакета "server-only" для vitest (node-окружение).
 * В приложении реальный "server-only" валит сборку при попадании
 * серверного модуля в клиентский бандл; в тестах он не нужен.
 */
export {};
