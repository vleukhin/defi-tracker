import {
  CATEGORIES,
  CATEGORY_LABEL,
  CATEGORY_UNIT,
} from "@/components/portfolio/category";

/**
 * Три фиксированные категории портфеля в порядке таблицы (S2.1).
 * Подписи и единицы живут в `components/portfolio/category` — там же,
 * где их цвета: категория на всех экранах называется одинаково.
 */
export const TRADE_CATEGORIES = CATEGORIES;

export { CATEGORY_LABEL, CATEGORY_UNIT };
